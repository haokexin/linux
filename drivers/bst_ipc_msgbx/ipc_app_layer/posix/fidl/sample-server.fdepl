//sample-server.fdepl
import "sample-inf.fdepl"

define com.bst.ipc.deployment for provider as SampleServer {
    ProviderType = Server
    RTE = BareMetal
    SrcEndID = CPU_7
    instance com.bst.ipc.sample.test {
    // Note: Server FID MUST be set to DEF
        FID = DEF
        SID = 1
    }
}